config BR2_PACKAGE_LDD
        bool "ldd"
        help
	  Includes the scull load, unload and module load,unload in the kernel image.
	  Used to understand buildroot external trees and package builds.
